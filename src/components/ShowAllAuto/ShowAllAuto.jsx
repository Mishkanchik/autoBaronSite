import { Box, Container } from "@mui/material";

const ShowAllAutoMain = () => {
	return (
		<Box
			sx={{
				backgroundColor: "#635985",
				minHeight: "100vh",
				display: "flex",
				flexDirection: "column",
				alignItems: "center",
				justifyContent: "center",
			}}>
			<Container></Container>
		</Box>
	);
};

export default ShowAllAutoMain;
